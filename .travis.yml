language: ruby
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.5
  - 2.2.0

script: bundle exec rspec
addons:
  code_climate:
    repo_token: $CODECLIMATE_REPO_TOKEN

# Use docker travis worker
sudo: false

cache: bundler

#!/usr/bin/ruby

$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'application_controller'

if __FILE__ == $PROGRAM_NAME
  application = ConsoleApplication.new
  application.launch
end
